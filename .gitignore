.claude/

# Documentation (local only, not needed on hosting)
DESIGN-SYSTEM.md
TYPOGRAPHY.md
Base info.md
CLAUDE.md
FORMS-CHECKLIST.md
SEO-ANALYTICS-CHECKLIST.md

# Local screenshot tool (Playwright) — only the entry script + node_modules,
# package.json теперь нужен для Vercel build (Supabase SDK, build-data script).
node_modules/
screenshot.js
screenshots/

# Local env (Supabase service_role key, никогда не коммитить)
.env
.env.local

# Admin setup docs
admin/SETUP.md

# Sensitive client data (PII — never commit)
*.xlsx
*.xls
Client*.xlsx

# Business/legal docs — served publicly if committed (outputDirectory="."), never commit
*.docx
Tender_Procedure_*.html
admin/BRAND_BOOK_BRIEF.md
memo_text.txt
.tmp_memo/

# Source photos (large originals → kept in D:\global-bali-docs\sources)
Mark.jpg
DSC*.jpg

# Playwright MCP cache
.playwright-mcp/

# Brand Book heavy assets (PDF + print sources too big for git/GitHub)
# Light assets (Logo, Icons, Fonts, Instagram previews) stay tracked.
BB/Guideline.pdf
BB/Print/
BB/Guideline.txt

# macOS metadata (junk)
.DS_Store
**/.DS_Store
