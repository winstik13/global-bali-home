{
  "name": "global-bali-home",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Build disabled — git is source of truth, run build:from-supabase manually if Supabase sync needed'",
    "build:from-supabase": "node scripts/build-data.mjs",
    "migrate-content": "node scripts/migrate-content.mjs",
    "screenshot": "node screenshot.js",
    "screenshot:index": "node screenshot.js index",
    "screenshot:projects": "node screenshot.js projects",
    "screenshot:admin": "node screenshot.js admin"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "playwright": "^1.44.0",
    "xlsx": "^0.18.5"
  }
}
